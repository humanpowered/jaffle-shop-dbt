version: 2

sources:
  - name: stripe
    database: dbt-tutorial
    tables:
      - name: payment
        config: 
          loaded_at_field: _batched_at
          freshness:
            warn_after:
              count: {count: 12, period: hour}
            error_after:
              count: {count: 24, period: hour}